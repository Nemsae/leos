import React from 'react';
import Paper from 'material-ui/Paper';

import './styles.css';

const Returns = props => (
  <Paper className='returns-container'>
    <h1>Returns</h1>
  </Paper>
);

export default Returns;
